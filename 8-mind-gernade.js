const num = 5
const num2 = 3

function addvalue () {

    console.log(`the add value is : ${num+num2}`)
}

addvalue()

//in app js  you can write  require(./mindgrenade) without assign it to a variable.
// the result will be the add value is : 8 